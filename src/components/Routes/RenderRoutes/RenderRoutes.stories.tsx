/* eslint-disable */

import { RenderRoutes } from './RenderRoutes';

export default {
  title: 'EventsSection',
};

export const Default = () => <RenderRoutes routes={[]} />;

Default.story = {
  name: 'default',
};
